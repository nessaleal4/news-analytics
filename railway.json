{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apt-get update && apt-get install -y python3 python3-pip && cd backend/app && pip3 install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
