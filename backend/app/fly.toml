app = "news-analytics-api"
primary_region = "iad"

[build.args]
  PYTHON_VERSION = "3.9.18"
  
[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true

[env]
  PORT = "8080"
  PYTHONUNBUFFERED = "1"
  DATA_DIR = "/app/data"
